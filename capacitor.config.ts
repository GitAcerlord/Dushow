import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.cosmictardigradepeek',
  appName: 'cosmic-tardigrade-peek',
  webDir: 'dist'
};

export default config;
